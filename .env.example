# Copy to .env — Supabase project (EU region). Both values are safe to ship in
# the client bundle; the anon key is gated by RLS + the upsert_participant RPC.
EXPO_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=eyJ...
